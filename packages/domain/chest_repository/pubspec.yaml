name: cchest_repository

environment:
  sdk: ">=3.1.0 <4.0.0"

dependencies:
  bobs_jobs: ^0.0.1-dev.3
  ccore:
  cdatabase_client:

dev_dependencies:
  flutter_test:
    sdk: flutter
  mocktail: ^1.0.4
  test_beautifier: ^1.1.0
  very_good_analysis: ^6.0.0
