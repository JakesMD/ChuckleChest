name: cplatform_client

environment:
  sdk: ">=3.1.0 <4.0.0"

dependencies:
  bobs_jobs: ^0.0.1-dev.4
  ccore:
  flutter:
    sdk: flutter
  image: ^4.2.0
  image_picker: ^1.1.2
  share_plus: ^10.0.2
  very_good_analysis: ^6.0.0
