/// Represents how a gem was shared.
enum CGemShareMethod {
  /// The gem was shared using the device's share dialog.
  dialog,

  /// The gem was shared by copying it to the clipboard.
  clipboard,
}
