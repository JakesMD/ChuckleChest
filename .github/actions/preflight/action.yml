name: Preflight check
description: Runs checks and simulates deploying the production version of the app.

runs:
  using: "composite"

  steps:
    - name: 🕵️ Run checks
      shell: bash
      run: hobnob ci SUPABASE_PROJECT_URL=${{ env.SUPABASE_PROJECT_URL }} SUPABASE_ANON_KEY=${{ env.SUPABASE_ANON_KEY }}

    - name: 🗃️ Push database changes (dry run)
      shell: bash
      run: hobnob db:push SUPABASE_PROJECT_ID=${{ env.SUPABASE_PROJECT_ID }} DRY_RUN=true